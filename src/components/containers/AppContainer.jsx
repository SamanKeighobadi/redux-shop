import React from 'react';
import Products from '../Products/Products';

const AppContainer = () => {
    return (
        <div>
            <Products />
        </div>
    );
};

export default AppContainer;